package com.logic.logistic.config;

/*@Configuration
@EnableWebSecurity*/
//public class SecurityConfig {
	/*
	 * @Bean public SecurityFilterChain filterChain(HttpSecurity http) throws
	 * Exception { http .authorizeHttpRequests(auth -> auth
	 * .requestMatchers("/login.html", "/css/**", "/js/**").permitAll() // public
	 * .anyRequest().authenticated() // everything else requires login )
	 * .formLogin(form -> form .loginPage("/login.html")
	 * .defaultSuccessUrl("/bookings", true) .permitAll() )
	 * 
	 * .sessionManagement(session -> session
	 * .invalidSessionUrl("/login?session=invalid") );
	 * 
	 * return http.build(); }
	 * 
	 * // Dummy bean to prevent spring auto-user generation
	 * 
	 * @Bean public UserDetailsService userDetailsService() { return username -> {
	 * throw new UsernameNotFoundException("User not managed by Spring Security");
	 * }; }
	 */
//}
